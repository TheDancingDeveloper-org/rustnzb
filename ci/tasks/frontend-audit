#!/bin/sh
set -eu
. "$(dirname "$0")/lib.sh"
trap cleanup_frontend EXIT
task_start frontend-audit node npm
frontend_dir=$REPO_ROOT/apps/rustnzb/frontend
npm --prefix "$frontend_dir" ci --no-audit --no-fund
npm --prefix "$frontend_dir" audit --audit-level=high
