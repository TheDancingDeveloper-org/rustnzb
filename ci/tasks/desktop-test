#!/bin/sh
set -eu
. "$(dirname "$0")/lib.sh"
. "$(dirname "$0")/cargo-auth"
trap 'cleanup_cargo_auth; cleanup_frontend' EXIT
task_start desktop-test cargo pkg-config patchelf
setup_cargo_auth
task_target_dir desktop-test
prepare_placeholder_frontend
cargo test --manifest-path desktop/src-tauri/Cargo.toml --all-targets --locked
show_sccache_stats
